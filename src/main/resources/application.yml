spring:
  profile:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/supportDesk?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
